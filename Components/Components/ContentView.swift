
import SwiftUI

struct ContentView: View {
  var body: some View {
    Text("Hello World, this is a test of things updating now that we have a product which is becoming easier and better to use continuously!")
    Text("We are going to change the code again here with some simple text changes and see if it does what we want it to do!")
    // write your Swift UI code here
  }
}
            
struct ContentView_Previews: PreviewProvider {
  static var previews: some View {
    ContentView()
  }
}